require 'spec_helper'

describe ParamsModifier do
  it 'has a version number' do
    expect(ParamsModifier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
